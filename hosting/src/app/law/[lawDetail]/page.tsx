import { NextPage } from "next";

interface Props {}

const IndexPage: NextPage<Props> = (props): JSX.Element => {
  return (
    <div>
      <button>a</button>
    </div>
  );
};

export default IndexPage;
