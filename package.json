{
  "name": "auth-raw-yuu-app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "client": "cd hosting && yarn install && yarn dev",
    "server": "cd functions && yarn install && yarn build",
    "emulator": "firebase emulators:start --only functions",
    "local": "concurrently \"yarn emulator\" \"yarn client\" \"yarn server\" "
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
